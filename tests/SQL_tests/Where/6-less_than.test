CREATE TABLE less_than_test (
    num INT,
    str VARCHAR(20)
);

INSERT INTO less_than_test (num, str)
VALUES (1, bruh);

INSERT INTO less_than_test (num, str)
VALUES (2, fr);

SELECT * FROM less_than_test WHERE num < 2;